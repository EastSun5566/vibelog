{
  "name": "@vibelog/app",
  "version": "0.5.0-beta.4",
  "private": true,
  "description": "Hosted HackMD-to-Astro theme editor",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/EastSun5566/vibelog.git",
    "directory": "packages/app"
  },
  "type": "module",
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "dev": "pnpm css:build && tsx --env-file=../../.env src/main.ts",
    "build": "rm -rf dist && tsc -p tsconfig.build.json && cp -R src/drizzle dist/drizzle && pnpm css:build --minify",
    "css:build": "tailwindcss -i src/styles/app.css -o dist/assets/app.css",
    "db:generate": "drizzle-kit generate --config drizzle.config.ts",
    "db:check": "drizzle-kit check --config drizzle.config.ts",
    "start": "node dist/main.js",
    "backup": "node dist/backup.js",
    "beta:funnel": "node dist/beta-funnel.js",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@hono/node-server": "^1.19.14",
    "@vibelog/core": "workspace:*",
    "better-auth": "1.6.23",
    "drizzle-orm": "0.45.2",
    "hono": "^4.12.30",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@tailwindcss/cli": "4.3.3",
    "@types/node": "^24.0.0",
    "basecoat-css": "1.0.2",
    "drizzle-kit": "0.31.10",
    "tailwindcss": "4.3.3",
    "tsx": "^4.19.4",
    "typescript": "7.0.2"
  }
}
