---
interface Props {
  date: Date;
}

const { date } = Astro.props;
---

<time datetime={(date || new Date()).toISOString()}>
  {
    (date || new Date()).toLocaleDateString("en-us", {
      year: "numeric",
      month: "short",
      day: "numeric",
    })
  }
</time>
