---
import { SITE_LANGUAGE } from "../consts";

interface Props {
  date: Date;
}

const { date } = Astro.props;
---

<time datetime={date.toISOString()}>
  {
    date.toLocaleDateString(SITE_LANGUAGE, {
      year: "numeric",
      month: "short",
      day: "numeric",
    })
  }
</time>
